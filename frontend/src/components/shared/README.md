# Shared Компоненты

Переиспользуемые компоненты общего назначения.

## Компоненты

### LoadingScreen

Полноэкранный экран загрузки с анимацией.

```tsx
import { LoadingScreen } from '@/components/shared';

// Базовое использование
<LoadingScreen />

// С кастомным текстом
<LoadingScreen
  title="Quiz Creator"
  subtitle="Загрузка данных..."
/>
```

**Props:**

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `title` | `string` | `'Quiz Creator'` | Заголовок |
| `subtitle` | `string` | `'Загрузка...'` | Подзаголовок |

---

### ErrorScreen

Полноэкранный экран ошибки с кнопкой действия.

```tsx
import { ErrorScreen } from '@/components/shared';

// Базовое использование
<ErrorScreen message="Не удалось загрузить данные" />

// С кастомными параметрами
<ErrorScreen
  title="Ошибка сети"
  message="Проверьте подключение к интернету"
  emoji="📡"
  actionLabel="Обновить"
  onAction={() => refetch()}
/>
```

**Props:**

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `message` | `string` | — | Текст ошибки (обязательный) |
| `title` | `string` | `'Ошибка подключения'` | Заголовок |
| `emoji` | `string` | `'😕'` | Эмодзи |
| `actionLabel` | `string` | `'Попробовать снова'` | Текст кнопки |
| `onAction` | `() => void` | `reload()` | Обработчик кнопки |

---

### PlaceholderPage

Заглушка для страниц в разработке.

```tsx
import { PlaceholderPage } from '@/components/shared';

<PlaceholderPage title="Предпросмотр" />

<PlaceholderPage
  title="Настройки"
  description="Скоро будет доступно"
  emoji="⚙️"
/>
```

**Props:**

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `title` | `string` | — | Заголовок (обязательный) |
| `description` | `string` | `'Страница в разработке'` | Описание |
| `emoji` | `string` | `'🚧'` | Эмодзи |

---

## Когда использовать

- **LoadingScreen** — начальная загрузка приложения, авторизация
- **ErrorScreen** — критические ошибки, требующие действия пользователя
- **PlaceholderPage** — незавершённые страницы в разработке
