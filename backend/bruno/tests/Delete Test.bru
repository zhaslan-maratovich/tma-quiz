meta {
  name: Delete Test
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/tests/:testId
  body: none
  auth: none
}

params:path {
  testId: <test-uuid-here>
}

headers {
  X-Dev-User-Id: {{devUserId}}
}
